<div class="card-trip">
  <%= image_tag @bookmark.movie.poster_url, alt: "Poster of #{@bookmark.movie.title}" %>
  <div class="card-trip-infos">
    <div>
      <h2><%= @bookmark.movie.title %> <i class="fas fa-star"></i> <%= @bookmark.movie.rating %></h2>
      <p><%= @bookmark.movie.overview %></p>
    </div>
      <p><i class="fas fa-comment"></i> <%= @bookmark.comment %></p>
  </div>
</div>
